// variables3.cairo
// Execute `starklings hint variables3` or use the `hint` watch subcommand for a hint.

use debug::PrintTrait;

fn main() {
    let x: felt252 = 100;
    println!("x is {}", x);
}
